import { } from "../../constans"


const initialState = {
    page: 1,

}

export default function generalReducer(state=initialState, {type,payload}){
    switch(type){
        
            
        default: 
            return {...state}
    }
}